import React, {useState } from 'react';
import Header from "../components/Header"

const Home = (props) => {
  return (
    <div id="body-container">
        <Header></Header>
        <h1>Teste</h1>
    </div>
  );
};

export default Home;
